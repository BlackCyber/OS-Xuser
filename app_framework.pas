{ app_framework.pas }

procedure RunApplication(AppName: String);
begin
  { Load and run the specified application }
end;

procedure RegisterApplication(AppName: String);
begin
  { Register a new application with the OS }
end;
